spec_id: <% tp.user.generateSpecId() %>
timestamp: <% tp.date.now("YYYY-MM-DD_HH-mm") %>
priority: low
status: proposed

title: [Integration Title]
description: [Brief description of the integration]

components:
  - name: [Component 1]
    version: [Version]
    interface: [[Interface Link]]
    
  - name: [Component 2]
    version: [Version]
    interface: [[Interface Link]]

dependencies:
  required: []
  optional: []

integration_points:
  - endpoint: [Endpoint Path]
    method: [HTTP Method]
    input_format: [Format]
    output_format: [Format]

performance_requirements:
  response_time: [Requirement]
  throughput: [Requirement]
  accuracy: [Requirement]

security_requirements:
  - [Requirement 1]
  - [Requirement 2]

testing_strategy:
  unit_tests: [Required/Optional]
  integration_tests: [Required/Optional]
  performance_tests: [Required/Optional]
  security_tests: [Required/Optional]
